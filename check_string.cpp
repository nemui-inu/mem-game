#include <stdio.h>
#include <conio.h>

void checker(int len)
{
    for(int i=1; i<len; i++)
    {
        len
    }   
}

int main(void)
{
    while (1){
        int len=0, val;
        char input[10], data[] = "abc";
        
        printf("Input: ");
        scanf("%s", &input);

        if (getch()=='q')
            break;
    }
}